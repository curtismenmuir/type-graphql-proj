# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

"""Supported Account Types"""
enum AccountTypes {
  Admin
  Instructor
  Customer
  Visitor
}

input AddRatingInput {
  title: String!
  rating: Float!
}

input CreateRecipeInput {
  title: String!
  description: String
}

input CreateUserInput {
  username: String!
  password: String!
  accountType: Float!
}

"""
The javascript `Date` as string. Type represents date and time as the ISO Date string.
"""
scalar DateTime

input LoginUserInput {
  username: String!
  password: String!
}

type Mutation {
  """Add a new recipe. Requires a logged in users access token."""
  addRecipe(recipe: CreateRecipeInput!): Recipe!

  """Add a new recipe. Requires an admin access token."""
  removeRecipe(title: String!): Boolean!

  """Add a new rating to a recipe. Requires a logged in users access token."""
  addRating(recipe: AddRatingInput!): Boolean!

  """Add new user. Requires admin access token."""
  addUser(user: CreateUserInput!): User!

  """Delete user. Requires admin access token."""
  removeUser(username: String!): Boolean!
}

type Query {
  """Get a specific recipe by its title"""
  recipe(title: String!): Recipe

  """Get all the recipes from around the world"""
  recipes: [Recipe!]!

  """Get User by username"""
  user(username: String!): User

  """Get all Users"""
  users: [User!]!

  """User Login"""
  loginUser(user: LoginUserInput!): String!
}

"""Object representing a cooking recipe"""
type Recipe {
  id: ID!
  title: String!
  specification: String @deprecated(reason: "Use `description` field instead")

  """The recipe description with preparation info"""
  description: String
  ratings: [Int!]!
  creationDate: DateTime!
  ratingsCount(minRate: Int = 0): Int!
  averageRating: Float
}

"""Object representing a users account details"""
type User {
  id: ID!
  username: String!

  """password"""
  password: String!

  """AccountType"""
  accountType: AccountTypes!
}
